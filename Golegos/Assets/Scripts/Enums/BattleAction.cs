using System.Collections;
using UnityEngine;

namespace Golegos.Enums {
    public enum BattleAction {
        Selecting,
        Attack,
        NormalAttack,
        SpecialAttack,
        Stats,
        QuickSave,
        Opponent
    }
}